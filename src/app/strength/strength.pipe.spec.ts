import { StrengthPipe } from './strength.pipe';

describe('StrengthPipe', () => {
    let pipe;

    beforeEach(() => {
      // Arrange
        pipe = new StrengthPipe();
    });

  //   it('should display weak if the strength is 5', () => {
  //       //ACT
  //       // Assert
  //       expect(pipe.transform(5))
  //       .toBe('5 (weak)');
  //   });
  //   it('should display strong if the strength is 10', () => {
  //     expect(pipe.transform(10))
  //     .toBe('10 (strong)')
  //   });
  // it('should display unbelievable if the strength is 20', () => {
  //     expect(pipe.transform(20))
  //       .toBe('20 (unbelievable)')
  //   });


});
